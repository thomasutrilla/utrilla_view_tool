require "spec_helper"

describe UtrillaViewTool do
  it "has a version number" do
    expect(UtrillaViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
